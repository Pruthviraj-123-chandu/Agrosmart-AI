# Gemini API Key
GEMINI_API_KEY=

# Port to run the server on (default: 3000)
PORT=3000

# Optional: OpenWeather API Key for live weather (fallback for manual entry if missing)
VITE_OPENWEATHER_API_KEY=
